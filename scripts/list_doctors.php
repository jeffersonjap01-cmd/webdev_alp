<?php

// This script was a temporary helper to list doctors during debugging.
// It has been retired and intentionally left blank to avoid accidental execution.
// You can safely delete this file if you no longer need it.

// If you want to recreate a safe listing, ensure you use the `status` field
// rather than the removed `is_active` accessor.

return;
